name: CD

on:
  push:
    tags:
      - 'v*.*.*'

env:
  NODE_VERSION: '22'
  PNPM_VERSION: '9'

jobs:
  deploy-staging:
    name: Deploy to Staging
    runs-on: ubuntu-latest
    timeout-minutes: 15
    environment: staging

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm turbo build

      - name: Deploy Frontend to Vercel (Staging)
        run: echo "TODO - Deploy frontend to Vercel staging"
        # Será implementado quando Vercel for configurado

      - name: Deploy API to Fly.io (Staging)
        run: echo "TODO - Deploy API to Fly.io staging"
        # Será implementado quando Fly.io for configurado

  deploy-production:
    name: Deploy to Production
    runs-on: ubuntu-latest
    timeout-minutes: 15
    needs: deploy-staging
    environment: production

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup pnpm
        uses: pnpm/action-setup@v4
        with:
          version: ${{ env.PNPM_VERSION }}

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: ${{ env.NODE_VERSION }}
          cache: 'pnpm'

      - name: Install dependencies
        run: pnpm install --frozen-lockfile

      - name: Build all packages
        run: pnpm turbo build

      - name: Deploy Frontend to Vercel (Production)
        run: echo "TODO - Deploy frontend to Vercel production"

      - name: Deploy API to Fly.io (Production)
        run: echo "TODO - Deploy API to Fly.io production"

      - name: Create GitHub Release
        uses: softprops/action-gh-release@v2
        with:
          generate_release_notes: true
